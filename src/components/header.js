import React from "react";

const Header =() => (
    <div className="sticky" style={{textAlign:'center', color:'white', backgroundColor:'teal'}}>
        <h4>New ToDo Application</h4>
        <p>by Joel Okoniha</p>
    </div>
);

export default Header;